# Simple File Renamer

Create a simple Python script that renames files in a directory.

Requirements:

- Rename files with a specific pattern
- Add timestamp to filename
- Handle basic error cases
- Simple command line interface

No complex features needed, just basic file operations.
