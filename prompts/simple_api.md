Simple REST API
Create a basic REST API with CRUD operations for managing users. The API should include endpoints for creating, reading, updating, and deleting users. Include proper error handling and input validation.
